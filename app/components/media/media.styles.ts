import { ImageStyle, ViewStyle } from "react-native"

export const mediaStyles = {
  WRAPPER: {
    justifyContent: "center",
  } as ViewStyle,

  PHOTO: {
    width: "33.33%",
    height: 150,
  } as ImageStyle,
}
